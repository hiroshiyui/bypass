{
  "name": "bypass-extension",
  "version": "0.1.0",
  "private": true,
  "description": "WebExtension popup for the bypass password manager.",
  "license": "GPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
