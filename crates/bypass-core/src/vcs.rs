//! Optional version-control abstraction. Linux CLI backs this with `git2`;
//! browser/Android may pass `None` and rely on external sync.

// TODO(0.5): define VcsError, Commit, and the VersionControl trait.
